Tablo Görüntüleme:

SELECT * FROM stok_yonetimi.users;

---

Tablo Oluşturma:

CREATE TABLE customers (
    Personid int NOT NULL AUTO_INCREMENT,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    Age int,
    PRIMARY KEY (Personid)
);

---

Yeni kullanıcı:

INSERT INTO users (userName,password)
VALUES ('berke','b1234');

---

Column ekleme:

ALTER TABLE users ADD column telefon varchar(25);

---

Column silme:

ALTER TABLE tablo_adi
DROP COLUMN kolon_adi;

---

Satıra veri ekleme:

"INSERT users (userName, password, isim, soyisim, email, telefon) VALUES ( '"+username+"','"+pass+"','"+user+"','"+lastname+"','"+email+"','"+phone+"')";

---

satırdan veri silme:

"DELETE FROM users WHERE PersonId="+userIdBox.Text+"";



